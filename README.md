# Ralph implementation spec bundle (v2) — Complete

All specs, Rust + Nix requirements, templates, schemas, and examples.

Contents:
- SPEC.md — normative spec
- schemas/ — JSON Schema Draft 2020-12
- templates/ — repo scaffolds (Nix flake.nix + Rust Cargo.toml)
- examples/ — sample PRD + docs + validation profiles
- docs/RUST_REQUIREMENTS.md — Rust ecosystem constraints
- docs/NIX_REQUIREMENTS.md — Nix packaging approach
